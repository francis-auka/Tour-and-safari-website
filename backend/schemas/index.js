const tour = require('./tour');
const destination = require('./destination');
const blogPost = require('./blogPost');
const service = require('./service');

module.exports = [tour, destination, blogPost, service];
